# 📚 Stack Implementation in Python

This project demonstrates how a **Stack (LIFO data structure)** works using:

- A **Python list** as a stack  
- A **custom Stack class** with full stack operations  

A **Stack** follows the **Last-In-First-Out (LIFO)** principle — the last inserted item is the first to be removed.

---

## 🔧 Stack Operations

| Operation | Description |
|----------|-------------|
| **Push** | Insert an element onto the stack |
| **Pop** | Remove and return the top element |
| **Peek** | View the top element without removing it |
| **isEmpty** | Check if the stack has no elements |
| **Size** | Return the number of elements in the stack |

